spring:
  application:
    name: KOMME-BE
  profiles:
    active: local

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:}

discord:
  alert:
    enabled: ${DISCORD_ALERT_ENABLED:false}
    webhook-url: ${DISCORD_WEBHOOK_URL:}
